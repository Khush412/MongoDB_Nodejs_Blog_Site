<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Sign Up | The Daily Dairy</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet" />
  <link rel="icon" type="image/png" href="/images/Site-icon.png" />
  <link href="/css/style.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body class="auth-page">
  <%- include('partials/nav') %>
    <main class="auth-main">
      <div class="auth-card">
        <h2 class="text-center mb-4 fw-bold" style="color:#ffd369; letter-spacing:.04em;">Create Account</h2>
        <% if (messages && messages.error) { %>
          <div class="alert alert-danger mb-3">
            <%= messages.error %>
          </div>
          <% } %>
            <% if (messages && messages.success) { %>
              <div class="alert alert-success mb-3">
                <%= messages.success %>
              </div>
              <% } %>
                <form action="/signup" method="POST" autocomplete="off">
                  <div class="mb-4">
                    <label for="name" class="form-label">Full Name</label>
                    <input type="text" id="name" name="name" class="form-control" placeholder="John Doe" required>
                  </div>
                  <div class="mb-4">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" id="email" name="email" class="form-control" placeholder="you@example.com"
                      required>
                  </div>
                  <div class="mb-4">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" id="password" name="password" class="form-control" placeholder="••••••••"
                      required>
                  </div>
                  <div class="mb-5">
                    <label for="confirmPassword" class="form-label">Confirm Password</label>
                    <input type="password" id="confirmPassword" name="confirmPassword" class="form-control"
                      placeholder="••••••••" required>
                  </div>
                  <button type="submit" class="btn btn-auth w-100 py-2 mb-2">Sign Up</button>
                </form>
                <div class="text-center my-3 divider-text">or</div>
                <!-- Only social icons in one row -->
                <div class="d-flex justify-content-center gap-3 mb-2 social-icon-row">
                  <a href="/auth/google" class="icon-circle-btn btn-google" title="Sign Up with Google">
                    <img src="/images/google-icon.png" alt="Google">
                  </a>
                  <a href="/auth/twitter" class="icon-circle-btn btn-twitter" title="Sign Up with Twitter">
                    <img src="/images/twitter-icon.png" alt="Twitter">
                  </a>
                  <a href="/auth/github" class="icon-circle-btn btn-github" title="Sign Up with GitHub">
                    <img src="/images/github-icon.png" alt="GitHub">
                  </a>
                </div>
                <div class="text-center mt-4 small" style="color:#eee;">
                  Already have an account?
                  <a href="/login" class="fw-bold highlight-link">Login</a>
                </div>
      </div>
    </main>
    <%- include('partials/footer') %>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
<nav class="navbar navbar-expand-lg navbar-dark shadow-sm site-navbar">
  <div class="container">
    <!-- Branding -->
    <a class="navbar-brand fw-bold fs-3" href="/" style="letter-spacing:.03em;">The Daily Dairy</a>

    <!-- Toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav links and content -->
    <div class="collapse navbar-collapse align-items-center" id="mainNav">
      <ul class="navbar-nav ms-4 align-items-center me-auto">
        <li class="nav-item"><a class="nav-link px-3" href="/">Home</a></li>
        <li class="nav-item"><a class="nav-link px-3" href="/myblogs">My Blogs</a></li>
          <% if (user && user.isAdmin) { %>
        <li class="nav-item">
          <a class="nav-link" href="/admin/dashboard">Admin</a>
        </li>
      <% } %>

      </ul>

      <!-- Search bar -->
      <form class="d-flex me-3 navbar-search" method="GET" action="/search">
        <input class="form-control me-2 rounded-pill" type="search" name="q" placeholder="Search blogs, authors, categories…" value="<%= typeof query !== 'undefined' ? query : '' %>">
        <button class="btn btn-light rounded-pill px-3" type="submit">Search</button>
      </form>

      <!-- Profile / Auth -->
      <% if (user) { %>
        <ul class="navbar-nav align-items-center">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
              <img src="<%= user.profilePic %>" alt="Profile" class="rounded-circle me-2" style="width:32px;height:32px;object-fit:cover;">
              <span>Hi, <%= user.name.split(' ')[0] %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userDropdown">
              <li><a class="dropdown-item" href="/profile">Profile</a></li>
              <li>
                <a class="dropdown-item" href="/bookmarks">My Bookmarks</a>
              </li>
              <li><a class="dropdown-item" href="/myblogs">My Blogs</a></li>
              <li><a class="dropdown-item" href="/blogs/new">Create Blog</a></li>
              
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
            </ul>
          </li>
        </ul>
      <% } else { %>
        <ul class="navbar-nav align-items-center">
          <li class="nav-item"><a class="nav-link" href="/signup">Signup</a></li>
          <li class="nav-item"><a class="nav-link" href="/login">Signin</a></li>
        </ul>
      <% } %>
      
    </div>
  </div>
</nav>

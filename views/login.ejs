<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login | The Daily Dairy</title>
  <link rel="icon" type="image/png" href="/images/Site-icon.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet" />
  <link href="/css/style.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body class="auth-page">
  <%- include('partials/nav') %>
    <main class="auth-main">
      <div class="auth-card">
        <h2 class="text-center mb-4 fw-bold" style="color:#ffd369; letter-spacing:.04em;">Sign In</h2>
        <% if (messages && messages.error) { %>
          <div class="alert alert-danger mb-3">
            <%= messages.error %>
          </div>
          <% } %>
            <form action="/login" method="POST" autocomplete="off">
              <div class="mb-4">
                <label for="email" class="form-label">Email</label>
                <input type="email" id="email" name="email" class="form-control" placeholder="you@example.com" required>
              </div>
              <div class="mb-4">
                <label for="password" class="form-label">Password</label>
                <input type="password" id="password" name="password" class="form-control" placeholder="••••••••"
                  required>
              </div>
              <button type="submit" class="btn btn-auth w-100 py-2 mb-2">Login</button>
            </form>
            <div class="text-center my-3 divider-text">or</div>
            <!-- Social icons row ONLY -->
            <div class="d-flex justify-content-center gap-3 mb-2 social-icon-row">
              <a href="/auth/google" class="icon-circle-btn btn-google" title="Login with Google">
                <img src="/images/google-icon.png" alt="Google">
              </a>
              <a href="/auth/twitter" class="icon-circle-btn btn-twitter" title="Login with Twitter">
                <img src="/images/twitter-icon.png" alt="Twitter">
              </a>
              <a href="/auth/github" class="icon-circle-btn btn-github" title="Login with GitHub">
                <img src="/images/github-icon.png" alt="GitHub">
              </a>
            </div>
            <div class="text-center mt-4 small" style="color: #eee;">
              Don't have an account?
              <a href="/signup" class="fw-bold highlight-link">Sign Up</a>
            </div>
      </div>
    </main>
    <%- include('partials/footer') %>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>